name: Notify Telegram on Merge to Main/Master

on:
  push:
    branches:
      - main
      - master

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Send Telegram Notification
        run: |
          curl -X POST "https://api.telegram.org/bot7540919637:AAGF4NuM4ZE5klD80vUltmu7vGm7B2DxlWY" \
            -d chat_id=244440953 \
            -d text="🚨 Merge to Main/Master Branch 🚨\nA merge has been made to the main/master branch. Please review the changes!"